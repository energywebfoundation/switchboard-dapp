import { ShowRawDataDirective } from './show-raw-data.directive';

describe('ShowRawDataDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowRawDataDirective();
    expect(directive).toBeTruthy();
  });
});
