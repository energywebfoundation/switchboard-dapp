import { ParseFieldValuePipe } from './parse-field-value.pipe';

describe('ParseFieldValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseFieldValuePipe();
    expect(pipe).toBeTruthy();
  });
});
