import { RetryBtnDirective } from './retry-btn.directive';

describe('RetryBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new RetryBtnDirective();
    expect(directive).toBeTruthy();
  });
});
